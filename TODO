Optimization:
  Collapse deterministic functions?
  Dynamic dependency graph?
    Every node has:
      static_deps
      dynamic_deps

Extend libraries:
  Vectors:
    IID(dist, dims)
    Joint(dist1, dist2...)
    Joint(dists)
